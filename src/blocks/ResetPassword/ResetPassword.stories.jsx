import React from 'react';

import { ResetPassword } from './ResetPassword';

export default {
  title: 'Client UI/blocks/ResetPassword',
  component: ResetPassword,
  argTypes: {},
};

const Template = (props) => <ResetPassword {...props} />;

export const Default = Template.bind({});
Default.args = {};
